# Required input for driving BGC
* ds_t, celcius
ds_s, psu
ds_u, ds_w, m/s
ds_sw, W/m2

# Optional input
ds_k, m2/s
ds_wind, m/s
ds_ice, 0 to 1 (ice fraction)

# Naming convention
* Dimension names must be `time`, `depth`, `lat`, and `lon`.